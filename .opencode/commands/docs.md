---description: Update docs after implementing machine features---
After implementing features for a machine: 1) Update `docs/<machine>.md` — move items from "Czego brakuje" to "Co już mamy" with [x]; 2) Update `src/video/text/DemoTextScene.ts` with demo content; 3) Run `npm test && npm run build`. Prompt for which machine changed.
