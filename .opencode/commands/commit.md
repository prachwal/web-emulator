---description: Stage, review, and commit changes---
Check `git status --short`, inspect `git diff --cached` (or diff for unstaged), review `git log --oneline -10` for style, then stage (`git add -A`) and commit with a message following the project convention (`feat(area): desc` or `fix(area): desc`). Show the final commit output.
