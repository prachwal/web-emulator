---description: Run full test suite then build---
Run the full test suite with `npm test` and then run the production build with `npm run build`. Show any failures or warnings. If any tests fail, analyze the failures and suggest fixes.
